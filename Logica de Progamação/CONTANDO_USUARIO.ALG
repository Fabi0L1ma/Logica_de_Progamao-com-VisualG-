Algoritmo "Contando_usuario"

Var

n1, contador, salto: inteiro

Inicio

contador <- 0

escreva("Quer que o programa conte até quanto: ")
leia(n1)

escreva("Digite o valor do salto: ")
leia(salto)

enquanto contador <= n1 faca
   escreva(contador)
   contador <- contador + salto
fimenquanto


Fimalgoritmo